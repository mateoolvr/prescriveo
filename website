<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="PRESCRIVEO - Révolutionnez votre santé avec une solution digitale complète et personnalisée.">
    <meta name="keywords" content="santé digitale, ordonnances électroniques, suivi médical, application santé">
    <meta name="author" content="PRESCRIVEO Team">
    <title>PRESCRIVEO - Votre Santé Connectée</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #007bff;
            --secondary: #1e2a44;
            --accent: #00d4b5;
            --light: #f5f7fa;
            --dark: #111827;
            --text: #2d3748;
            --shadow: 0 15px 40px rgba(0, 0, 0, 0.12);
            --gradient: linear-gradient(135deg, #007bff 0%, #00d4b5 100%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
            scroll-behavior: smooth;
        }

        body {
            background: var(--light);
            color: var(--text);
            line-height: 1.7;
            overflow-x: hidden;
        }

        /* Header */
        header {
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(12px);
            padding: 1rem 4rem;
            transition: all 0.3s ease;
        }

        header.scrolled {
            padding: 0.8rem 4rem;
            box-shadow: var(--shadow);
        }

        .nav-container {
            max-width: 1440px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--secondary);
            letter-spacing: 1px;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        nav ul {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        nav a {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            padding: 0.5rem 1rem;
            transition: all 0.3s ease;
        }

        nav a:hover {
            color: var(--primary);
            background: rgba(0, 123, 255, 0.1);
            border-radius: 20px;
        }

        .menu-btn {
            display: none;
            font-size: 1.8rem;
            background: none;
            border: none;
            cursor: pointer;
            color: var(--text);
        }

        /* Hero */
        .hero {
            min-height: 100vh;
            background: var(--gradient);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
            padding: 4rem;
        }

        .hero-background {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://source.unsplash.com/1920x1080/?healthcare') center/cover no-repeat;
            opacity: 0.2;
            z-index: 1;
        }

        .hero-content {
            z-index: 2;
            max-width: 900px;
            position: relative;
        }

        .hero h1 {
            font-size: 4.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
        }

        .hero p {
            font-size: 1.5rem;
            margin-bottom: 2.5rem;
            opacity: 0.9;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            padding: 1rem 3rem;
            background: white;
            color: var(--primary);
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

        .btn i {
            margin-left: 0.5rem;
        }

        .btn:hover {
            background: var(--primary);
            color: white;
            transform: scale(1.05);
        }

        .btn.secondary {
            background: transparent;
            border: 2px solid white;
            color: white;
            margin-left: 1.5rem;
        }

        /* Features */
        .section {
            padding: 8rem 4rem;
            max-width: 1440px;
            margin: 0 auto;
        }

        .section-title {
            font-size: 3rem;
            text-align: center;
            margin-bottom: 5rem;
            color: var(--secondary);
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 3rem;
        }

        .feature-card {
            background: white;
            padding: 3rem;
            border-radius: 25px;
            text-align: center;
            box-shadow: var(--shadow);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: var(--gradient);
            opacity: 0;
            transition: all 0.4s ease;
            transform: rotate(30deg);
        }

        .feature-card:hover::before {
            opacity: 0.1;
        }

        .feature-card:hover {
            transform: translateY(-15px);
        }

        .feature-card i {
            font-size: 3.5rem;
            color: var(--primary);
            margin-bottom: 1.5rem;
        }

        /* Testimonials */
        .testimonials {
            background: var(--dark);
            color: white;
            padding: 8rem 4rem;
            position: relative;
        }

        .testimonial-slider {
            max-width: 1000px;
            margin: 0 auto;
            overflow: hidden;
            position: relative;
        }

        .testimonial {
            text-align: center;
            padding: 2rem;
            opacity: 0;
            position: absolute;
            width: 100%;
            transition: all 0.5s ease;
        }

        .testimonial.active {
            opacity: 1;
            position: relative;
        }

        .testimonial p {
            font-size: 1.3rem;
            margin-bottom: 1rem;
            font-style: italic;
        }

        /* Stats */
        .stats {
            background: var(--gradient);
            color: white;
            padding: 6rem 4rem;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 3rem;
            max-width: 1440px;
            margin: 0 auto;
        }

        .stat-item h3 {
            font-size: 3rem;
            margin-bottom: 0.8rem;
            font-weight: 700;
        }

        /* Contact */
        .contact-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            background: white;
            padding: 4rem;
            border-radius: 25px;
            box-shadow: var(--shadow);
        }

        .contact-info h3 {
            color: var(--secondary);
            margin-bottom: 1.5rem;
        }

        .contact-form input,
        .contact-form textarea {
            width: 100%;
            padding: 1.2rem;
            margin-bottom: 1.5rem;
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .contact-form input:focus,
        .contact-form textarea:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(0, 123, 255, 0.1);
            outline: none;
        }

        /* Footer */
        footer {
            background: var(--dark);
            color: white;
            padding: 5rem 4rem 2rem;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            max-width: 1440px;
            margin: 0 auto 4rem;
        }

        .footer-grid a {
            color: #a1a1aa;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-grid a:hover {
            color: var(--accent);
        }

        .social-links a {
            color: white;
            font-size: 1.8rem;
            margin: 0 1rem;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            color: var(--accent);
            transform: translateY(-3px);
        }

        /* Back to Top */
        .back-to-top {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 50px;
            height: 50px;
            background: var(--primary);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: all 0.3s ease;
            cursor: pointer;
            box-shadow: var(--shadow);
        }

        .back-to-top.visible {
            opacity: 1;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .contact-container {
                grid-template-columns: 1fr;
            }
            .hero h1 {
                font-size: 3.5rem;
            }
        }

        @media (max-width: 768px) {
            .menu-btn {
                display: block;
            }
            nav ul {
                display: none;
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100vh;
                background: white;
                flex-direction: column;
                padding: 5rem 2rem;
                z-index: 999;
            }
            nav ul.active {
                display: flex;
            }
            .hero h1 {
                font-size: 2.5rem;
            }
            .hero p {
                font-size: 1.2rem;
            }
            .section {
                padding: 5rem 2rem;
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header role="banner">
        <div class="nav-container">
            <a href="#" class="logo" aria-label="PRESCRIVEO Accueil">PRESCRIVEO</a>
            <button class="menu-btn" aria-label="Ouvrir le menu"><i class="fas fa-bars"></i></button>
            <nav role="navigation">
                <ul>
                    <li><a href="#accueil">Accueil</a></li>
                    <li><a href="#fonctionnalites">Fonctionnalités</a></li>
                    <li><a href="#temoignages">Témoignages</a></li>
                    <li><a href="#stats">Stats</a></li>
                    <li><a href="#contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Hero -->
    <section class="hero" id="accueil">
        <div class="hero-background"></div>
        <div class="hero-content">
            <h1>Révolutionnez Votre Santé</h1>
            <p>Une solution digitale innovante pour gérer votre bien-être au quotidien.</p>
            <div>
                <a href="#contact" class="btn">Découvrir <i class="fas fa-arrow-right"></i></a>
                <a href="#fonctionnalites" class="btn secondary">Nos Services</a>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section class="section" id="fonctionnalites">
        <h2 class="section-title">Nos Fonctionnalités</h2>
        <div class="features-grid">
            <div class="feature-card">
                <i class="fas fa-file-medical"></i>
                <h3>Ordonnances Digitales</h3>
                <p>Accédez et gérez vos prescriptions en ligne avec une sécurité optimale.</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-bell"></i>
                <h3>Rappels Intelligents</h3>
                <p>Notifications personnalisées pour vos prises de médicaments.</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-user-md"></i>
                <h3>Suivi Médical</h3>
                <p>Connectez-vous directement avec vos professionnels de santé.</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-chart-line"></i>
                <h3>Analyse Santé</h3>
                <p>Visualisez vos progrès avec des rapports détaillés.</p>
            </div>
        </div>
    </section>

    <!-- Testimonials -->
    <section class="testimonials" id="temoignages">
        <h2 class="section-title">Ce Qu'ils Disent</h2>
        <div class="testimonial-slider">
            <div class="testimonial active">
                <p>"PRESCRIVEO a transformé ma gestion de santé quotidienne. Simple et efficace !"</p>
                <h4>— Sophie L., 34 ans</h4>
            </div>
            <div class="testimonial">
                <p>"Un outil indispensable pour rester organisé et en bonne santé."</p>
                <h4>— Marc D., 52 ans</h4>
            </div>
        </div>
    </section>

    <!-- Stats -->
    <section class="stats" id="stats">
        <div class="stats-grid">
            <div class="stat-item">
                <h3>50K+</h3>
                <p>Utilisateurs</p>
            </div>
            <div class="stat-item">
                <h3>98%</h3>
                <p>Satisfaction</p>
            </div>
            <div class="stat-item">
                <h3>24/7</h3>
                <p>Support</p>
            </div>
            <div class="stat-item">
                <h3>500+</h3>
                <p>Partenaires</p>
            </div>
        </div>
    </section>

    <!-- Contact -->
<section class="section" id="contact">
    <h2 class="section-title">Contactez-Nous</h2>
    <div class="contact-container">
        <div class="contact-info">
            <h3>Restez en Contact</h3>
            <p>Email: <a href="mailto:contact@prescriveo.fr">contact@prescriveo.fr</a></p>
            <p>Tél: <a href="tel:+33123456789">01 23 45 67 89</a></p>
            <p>123 Rue de la Santé, 75000 Paris</p>
        </div>
        <form class="contact-form" action="https://formspree.io/f/movjgznz" method="POST">
            <input type="text" name="name" placeholder="Votre Nom" required aria-label="Nom">
            <input type="email" name="email" placeholder="Votre Email" required aria-label="Email">
            <textarea name="message" placeholder="Votre Message" rows="5" required aria-label="Message"></textarea>
            <button type="submit" class="btn">Envoyer <i class="fas fa-paper-plane"></i></button>
        </form>
    </div>
</section>

    <!-- Footer -->
    <footer>
        <div class="footer-grid">
            <div>
                <h3 class="logo">PRESCRIVEO</h3>
                <p>Votre santé, connectée et simplifiée.</p>
            </div>
            <div>
                <h3>Navigation</h3>
                <p><a href="#accueil">Accueil</a></p>
                <p><a href="#fonctionnalites">Fonctionnalités</a></p>
                <p><a href="#contact">Contact</a></p>
            </div>
            <div>
                <h3>Légal</h3>
                <p><a href="#">Confidentialité</a></p>
                <p><a href="#">Conditions</a></p>
            </div>
        </div>
        <div class="social-links">
            <a href="#" aria-label="Facebook"><i class="fab fa-facebook"></i></a>
            <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
            <a href="#" aria-label="Instagram"><i class="fab fa-instagram"></i></a>
        </div>
        <p>© 2025 PRESCRIVEO. Tous droits réservés.</p>
    </footer>

    <!-- Back to Top -->
    <button class="back-to-top" aria-label="Retour en haut"><i class="fas fa-arrow-up"></i></button>

    <script>
        // Header scroll effect
        const header = document.querySelector('header');
        window.addEventListener('scroll', () => {
            header.classList.toggle('scrolled', window.scrollY > 50);
        });

        // Menu toggle
        const menuBtn = document.querySelector('.menu-btn');
        const nav = document.querySelector('nav ul');
        menuBtn.addEventListener('click', () => {
            nav.classList.toggle('active');
            menuBtn.innerHTML = nav.classList.contains('active') ? '<i class="fas fa-times"></i>' : '<i class="fas fa-bars"></i>';
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', e => {
                e.preventDefault();
                document.querySelector(anchor.getAttribute('href')).scrollIntoView({ behavior: 'smooth' });
                nav.classList.remove('active');
                menuBtn.innerHTML = '<i class="fas fa-bars"></i>';
            });
        });

        // Form handling with animation
        const form = document.querySelector('.contact-form');
        form.addEventListener('submit', async e => {
            e.preventDefault();
            const formData = new FormData(form);
            const data = Object.fromEntries(formData);

            if (!data.email.match(/^\S+@\S+\.\S+$/)) {
                alert('Veuillez entrer un email valide');
                return;
            }

            const submitBtn = form.querySelector('button');
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Envoi...';
            try {
                await new Promise(resolve => setTimeout(resolve, 1000));
                alert('Message envoyé avec succès !');
                form.reset();
                submitBtn.innerHTML = 'Envoyer <i class="fas fa-paper-plane"></i>';
            } catch (error) {
                alert('Erreur lors de l\'envoi.');
                submitBtn.innerHTML = 'Envoyer <i class="fas fa-paper-plane"></i>';
            }
        });

        // Animation Observer
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.feature-card, .stat-item, .testimonial').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.6s ease';
            observer.observe(el);
        });

        const style = document.createElement('style');
        style.textContent = '.visible { opacity: 1 !important; transform: translateY(0) !important; }';
        document.head.appendChild(style);

        // Testimonial Slider
        const testimonials = document.querySelectorAll('.testimonial');
        let current = 0;
        setInterval(() => {
            testimonials[current].classList.remove('active');
            current = (current + 1) % testimonials.length;
            testimonials[current].classList.add('active');
        }, 5000);

        // Back to Top
        const backToTop = document.querySelector('.back-to-top');
        window.addEventListener('scroll', () => {
            backToTop.classList.toggle('visible', window.scrollY > 300);
        });
        backToTop.addEventListener('click', () => {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
    </script>
</body>
</html>
